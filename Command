# sub_test 全量 + 本地模型（vLLM，需 py311 等 Python>=3.10 且 pip install vllm）
# 选卡：CUDA_VISIBLE_DEVICES=0  多卡并行：CUDA_VISIBLE_DEVICES=0,1,2 --tensor-parallel-size 3
MODEL=~/lzh/Qwen2.5-Coder-7B-Instruct

# conda activate py311
# export CUDA_VISIBLE_DEVICES=0

for f in datasets/xcodeeval/sub_test/LLVMIR.jsonl; do
  echo "=== EVAL $f ==="
  python datasets/xcodeeval/eval.py \
    --data "$f" \
    --model-type local \
    --model "$MODEL" \
    --paradigm direct \
    --llm-timeout 600 \
    --resume
done

# 结果目录: results/local_Qwen2___5-Coder-3B-Instruct/<语言>/*.txt

# ---------- API 轮换（本地 freeapi/，不提交 git）----------
# gpt-5.4：每节点/key 5 次后切换；gpt-5-mini：200 次（见 rotation_limits.py）
 for f in datasets/xcodeeval/sub_test/LLVMIR.jsonl; do
   python datasets/xcodeeval/eval.py --data "$f" --model gpt-5.4 --rotate --resume
done
#
# ---------- API 直连 chshapi（不轮换）----------
# export OPENAI_API_KEY='sk-...'
# for f in datasets/xcodeeval/sub_test/*.jsonl; do
#   python datasets/xcodeeval/eval.py \
#     --data "$f" --model gpt-5-mini --no-rotate \
#     --base-url https://api.chshapi.org \
#     --result-dir results/direct_GPT_5-mini --resume
# done
#
# ---------- 评测已有代码（各语言通过率）----------
  python datasets/xcodeeval/eval.py \
  --score-dir results/direct_Qwen2.5-Coder-7B-Instruct \
  --data-root datasets/xcodeeval/sub_test \


# 新跑评测（默认已是 index 文件名）
python datasets/xcodeeval/eval.py --data datasets/xcodeeval/sub_test/Python.jsonl ...

# 评测已有结果（score 模式同样认 index 文件名）
python datasets/xcodeeval/eval.py --score-dir results/direct_GPT_5-mini --data-root datasets/xcodeeval/sub_test

# 查第 5 题对应的真实 bug_code_uid
python datasets/xcodeeval/sub_test_map.py lookup --lang Python --index 5 --id-field bug_code_uid

# ---------- 训练（推荐：1×vLLM 批量推理 + HF LoRA 训 solver）----------
# 7 卡物理 1-7（不用 GPU0）：
#   CUDA_VISIBLE_DEVICES=1,2,3,4,5,6,7
#   vLLM tp 须整除 28（1/2/4/7），不能用 6
#   推荐：cuda:0-3 → vLLM(tp=4)，cuda:6 → solver 训练（物理卡 7）
#
# 依赖：pip install vllm peft torch transformers

cd ~/lzh/apr

# APPS
CUDA_VISIBLE_DEVICES=1,2,3,4,5,6,7 python model/train.py \
  --dataset apps \
  --dataset_path ~/lzh/datasets/APPS/train \
  --model_path ~/lzh/Qwen2.5-Coder-7B-Instruct \
  --chat_backend vllm \
  --vllm_tp_size 4 \
  --devices cuda:6 \
  --use_lora \
  --gradient_checkpointing \
  --inject_mode half \
  --inject_value 10 \
  --exec_timeout 30 \
  --exec_workers 8 \
  --output_dir outputs/solver_rl \
  --naive_bestofn 3 \
  --solver_bestofn 3 \
  --input_count 10 \
  --input_source auto \
  --start 0 --end 5000 \
  --epochs 1 \
  --save_every 50 \
  --val_size 300 \
  --val_every 25 \
  --val_seed 42


# ---------- LoRA only_solver pass@1（先合并，再 vLLM）----------
# 1) 仅合并（约 5-10 分钟，额外 ~15GB → outputs/only_solver/merged_model）
python model/merge_lora.py \
  --gpu 7 \
  --base-model ~/lzh/Qwen2.5-Coder-7B-Instruct \
  --lora ~/lzh/apr/outputs/only_solver/final \
  --output ~/lzh/apr/outputs/only_solver/merged_model

# 2) 合并 + vLLM 评测（无合并产物时自动 merge）
python model/run_pass1_only_solver_merged.py

# 结果: outputs/pass1_eval/merged_model_solver.jsonl
# 慢速回退（HF+PEFT）: python model/eval_pass1_lora.py ...

# ---------- 逐题推理（推理链 + naive/solver 代码 → <题号>/apr/infer_reasoning.jsonl）----------
# 挂物理卡 5：
python model/infer_reasoning.py \
  --gpu 5 \
  --dataset apps \
  --dataset_path ~/lzh/datasets/APPS/train \
  --model ~/lzh/Qwen2.5-Coder-7B-Instruct \
  --model-type local \
  --tensor-parallel-size 1 \
  --start 0 --end 5000 \
  --roles naive,solver \
  --resume

# CodeContests 预解压（训练/验证前跑一次，避免每题解 tar）
# cd ~/datasets/codecontests   # 服务器路径按实际改
# python extract_parquet_tasks.py tasks.parquet ./extracted_tasks
# 9644 题，约 1～2 分钟（本地 SSD）

# CodeContests 全量训练（物理卡 3,4,5,6：vLLM tp=2 占 cuda:0,1，solver 训 cuda:2）
CUDA_VISIBLE_DEVICES=4,5,6,7 python model/train.py \
  --dataset apps \
  --dataset_path ~/lzh/datasets/APPS/train \
  --model_path ~/lzh/Qwen2.5-Coder-7B-Instruct \
  --chat_backend vllm \
  --vllm_tp_size 2 \
  --devices cuda:2,cuda:3 \
  --use_lora \
  --gradient_checkpointing \
  --inject_mode half \
  --inject_value 10 \
  --exec_timeout 30 \
  --exec_workers 8 \
  --output_dir outputs/naive_solver_rl \
  --naive_bestofn 3 \
  --solver_bestofn 3 \
  --input_count 10 \
  --input_source auto \
  --start 0 --end 5000 \
  --epochs 1 \
  --save_every 50 \
  --val_size 300 \
  --val_every 25 \
  --val_seed 42
# CodeContests 全量 pass@1（物理卡 3,4,5,6，vLLM tp=4）
# python model/kill_pass1_gpu.py   # 若旧进程占卡先清理
# python model/run_pass1_codecontests_gpu3456.py
# 结果: outputs/pass1_eval_codecontests/*_summary.json

# ---------- CURE_codecontests 训练（train split 4529 题，json 直读免解压）----------
# 数据：~/datasets/CURE_codecontests/{train,test}/
# rollout 测例=test_input（隐藏测例），Public Test=example_input（题干样例）
CUDA_VISIBLE_DEVICES=4,5,6,7 python model/train.py \
  --dataset cure_codecontests \
  --dataset_path ~/datasets/CURE_codecontests \
  --model_path ~/lzh/Qwen2.5-Coder-7B-Instruct \
  --chat_backend vllm \
  --vllm_tp_size 2 \
  --devices cuda:2,cuda:3 \
  --use_lora \
  --gradient_checkpointing \
  --inject_mode half \
  --inject_value 10 \
  --exec_timeout 30 \
  --exec_workers 8 \
  --output_dir outputs/naive_solver_rl_cure_cc \
  --naive_bestofn 16 \
  --solver_bestofn 16 \
  --input_count 10 \
  --input_source input_output \
  --epochs 1 \
  --save_every 50 \
  --val_size 300 \
  --val_every 25 \
  --val_seed 42

# ---------- alldatasets 统一评测（物理卡 1）----------
# 数据：~/lzh/datasets/{LiveCodeBench,codeforces}/test/
# codeforces: test/test-00000-of-00001.parquet
MODEL=~/lzh/Qwen2.5-Coder-7B-Instruct

# LiveCodeBench
python alldatasets/eval.py \
  --dataset livecodebench \
  --model-type local \
  --model "$MODEL" \
  --gpu 1 \
  --tensor-parallel-size 1 \
  --resume

# CodeForces
python alldatasets/eval.py \
  --dataset codeforces \
  --model-type local \
  --model "$MODEL" \
  --gpu 1 \
  --tensor-parallel-size 1 \
  --resume

# CodeContests train 全量 pass@1 + BoN（tests/test_data.json 打分）
python alldatasets/eval.py \
  --dataset codecontests \
  --dataset-path ~/lzh/datasets/codecontests/extracted_tasks \
  --model-type local \
  --model "$MODEL" \
  --solver-bestofn 16 \
  --gpu 1 \
  --tensor-parallel-size 1 \
  --resume
# 结果: results/eval_codecontests_local_Qwen2.5-Coder-7B-Instruct/
#   passed_pass1.txt / passed_bon.txt / eval_report.json

# ---------- LoRA checkpoint 评测（基座 + adapter，免合并）----------
CKPT=~/lzh/apr/outputs/naive_solver_rl_cc_tests

# LiveCodeBench（solver LoRA）
python alldatasets/eval.py \
  --dataset livecodebench \
  --model-type local \
  --model "$MODEL" \
  --lora "$CKPT/final" \
  --gpu 1 \
  --tensor-parallel-size 1 \
  --resume

# CodeForces（solver LoRA）
python alldatasets/eval.py \
  --dataset codeforces \
  --model-type local \
  --model "$MODEL" \
  --lora "$CKPT/final" \
  --gpu 1 \
  --tensor-parallel-size 1 \
  --resume

# naive LoRA: --lora "$CKPT/final/naive" --prompt-file prompt/naivesolver.txt
# 中间 step: --lora "$CKPT/step_500"
# 训练 lora_r > 64 时需加 --max-lora-rank <r>
# 结果目录自动带 lora 后缀: results/eval_<ds>_local_<model>_lora_naive_solver_rl_cc_tests_final/

# ---------- CURE_codecontests 评测（test split 239 题，默认 test_input 打分）----------
# 基座 pass@1 + BoN
python alldatasets/eval.py \
  --dataset cure_codecontests \
  --dataset-path ~/datasets/CURE_codecontests \
  --model-type local \
  --model "$MODEL" \
  --solver-bestofn 16 \
  --gpu 1 \
  --tensor-parallel-size 1 \
  --resume

# LoRA checkpoint
python alldatasets/eval.py \
  --dataset cure_codecontests \
  --dataset-path ~/datasets/CURE_codecontests \
  --model-type local \
  --model "$MODEL" \
  --lora "$CKPT/final" \
  --gpu 1 \
  --tensor-parallel-size 1 \
  --resume
# --split train 可评 train split；--use-public-only 只用题干样例打分
# 结果: results/eval_cure_codecontests_local_<model>_test/
￼



给我命令

Explored
2 files
物理卡 4、5、6、7（naive+solver 双训，4 卡方案）：

cd ~/lzh/apr
CUDA_VISIBLE_DEVICES=4,5,6,7 python model/train.py \
  --dataset apps \
  --dataset_path ~/lzh/datasets/APPS/train \
  --model_path ~/lzh/Qwen2.5-Coder-7B-Instruct \
  --chat_backend vllm \
  --vllm_tp_size 2 \
  --devices cuda:2,cuda:3 \
  --use_lora \
  --gradient_checkpointing \
  --inject_mode half \
  --inject_value 10 \
  --exec_timeout 30 \
  --exec_workers 8 \
  --output_dir outputs/naive_solver_rl \
  --naive_bestofn 3 \
  --solver_bestofn 3 \
  --input_count 10 \
  --input_source auto \
  --start 0 --end 5000 \
  --epochs 1 \
  --save_every 50 \
  --val_size 300 \
  --val_every 25 \
  --val_seed 42
  --tensor-parallel-size 1 \
  --resume

CUDA_VISIBLE_DEVICES=4,5,6,7 python model/train.py \
  --dataset codecontests \
  --dataset_path /home/liuzhihao/datasets/codecontests/extracted_tasks \
  --dataset_io_source sample \
  --model_path ~/lzh/Qwen2.5-Coder-7B-Instruct \
  --chat_backend vllm \
  --vllm_tp_size 2 \
  --devices cuda:2,cuda:3 \
  --use_lora \
  --gradient_checkpointing \
  --inject_mode half \
  --inject_value 10 \
  --exec_timeout 30 \
  --exec_workers 8 \
  --input_source trigger \
  --input_count 10 \
  --output_dir outputs/naive_solver_rl_cc_trigger \
  --naive_bestofn 3 \
  --solver_bestofn 3 \
  --start 0 --end 1 \
  --epochs 1 \
  --save_every 500 \
  --val_size 100 \
  --val_every 500 \
  --val_seed 42