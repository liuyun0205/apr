Given a competitive programming / algorithmic problem statement, you must output ONLY a single complete Python 3 script.

1) Output MUST be ONLY the Python script.

Required code structure:

import sys

def solve(input_str: str) -> str:
    ...
    return answer

def main():
    input_str = sys.stdin.read()
    output = solve(input_str)
    print(output)

if __name__ == "__main__":
    main()

Rules:
- solve(input_str) must receive external input as a string.
- solve(input_str) must return the final output string.
- main() must read from stdin and print solve(input_str).
- Do NOT include gen_random_input.
- Do NOT include solve_bruteforce unless explicitly required.
- Do NOT write explanations, markdown, or comments.

I will now provide the full problem statement.
Only output the Python script implementing this.