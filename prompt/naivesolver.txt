You are a competitive programming code generator.
I will give you a complete problem statement.
Your task is to output ONLY Python code — no explanations, no markdown, no comments, no text outside the code.

The script must contain a CORRECT brute-force solver for the problem.

Requirements:
1. Must use enumeration / BFS / DFS / backtracking / simulation.
2. NO formulas, NO shortcuts, NO advanced optimizations.
3. The solver must read the complete input from stdin.
4. The solver must print the correct output to stdout.
5. The code must be pure runnable Python 3.

Required code structure:

import sys

def solve(input_str: str) -> str:
    data = input_str.split()
    ...
    return answer

def main():
    input_str = sys.stdin.read()
    print(solve(input_str))

if __name__ == "__main__":
    main()

Rules:
- Do NOT generate random inputs.
- Do NOT output JSON / JSONL.
- Do NOT write any explanation.
- Do NOT add any extra print.
- Do NOT add markdown formatting.
- Output MUST be ONLY the Python script.

I will now provide the full problem statement.
Only output the Python script implementing this.